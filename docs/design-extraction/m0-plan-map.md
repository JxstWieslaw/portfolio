# M0 Foundations — Plan Map, Design Delta, Task 1 Verification

Sources read in full:
- Plan: `c:\Users\wiesl\OneDrive\Documents\Github\JxstWieslaw\portfolio\docs\superpowers\plans\2026-08-15-m0-foundations.md` (4335 lines)
- Design: `...\docs\Portfolio Design\Foundations.dc.html` (552 lines, read in full)
- Design: `...\docs\Portfolio Design\Home.dc.html` (911 lines, markup read in full; script tail skimmed)
- Cross-check: `...\docs\Portfolio Design\Home v1.dc.html` (grepped — it is the *older* revision that matches the plan's positioning)
- Disk: `portfolio/` working tree

---

## Part A — Task map (19 tasks)

Git repo root is `C:\Users\wiesl\OneDrive\Documents\Github\JxstWieslaw`. All paths below are relative to `portfolio/` (the pnpm workspace root), which is a **subdirectory** of the git root.

| # | Title | Creates | Modifies | Depends on | Kind |
|---|---|---|---|---|---|
| 1 | Monorepo foundation | `package.json`, `pnpm-workspace.yaml`, `turbo.json`, `.npmrc`, `.nvmrc`, `.gitignore`, `packages/config/package.json`, `packages/config/tsconfig.base.json`, `packages/config/eslint.config.mjs` | — | none | **Structural** |
| 2 | Content schemas | `packages/contracts/package.json`, `packages/contracts/tsconfig.json`, `packages/contracts/eslint.config.mjs`, `packages/contracts/src/content.ts`, `packages/contracts/src/index.ts`, `packages/contracts/src/content.test.ts` | — | T1 | **Structural** (but `formationIdSchema` and `domainSchema.accent` are design-bearing — see Delta D-06/D-07) |
| 3 | `apps/web` scaffold — Next.js, tokens, fonts | `apps/web/package.json`, `apps/web/tsconfig.json`, `apps/web/next.config.ts`, `apps/web/postcss.config.mjs`, `apps/web/eslint.config.mjs`, `apps/web/vitest.config.ts`, `apps/web/tests/setup.ts`, `apps/web/app/globals.css`, `apps/web/app/fonts.ts`, `apps/web/app/layout.tsx`, `apps/web/app/page.tsx`, `apps/web/tests/unit/tokens.test.ts` | — | T1, T2 | **Visual** (`globals.css`, `fonts.ts`) + structural |
| 4 | Content files + typed loader | `content/profile.json`, `content/domains.json`, `content/projects.json`, `content/experience.json`, `content/skills.json`, `content/writing.json`, `apps/web/lib/content.ts`, `apps/web/tests/unit/content.test.ts` | — | T2 | **Structural** (but the JSON *is* the site copy — editorial) |
| 5 | Placeholder detection + linter | `apps/web/lib/placeholders.ts`, `apps/web/scripts/lint-content.ts`, `apps/web/tests/unit/placeholders.test.ts` | — | T4 | **Structural** |
| 6 | Layout primitives | `apps/web/components/layout/Container.tsx`, `Section.tsx`, `SkipLink.tsx`, `apps/web/tests/unit/layout.test.tsx` | — | T2 | **Visual** |
| 7 | `PosterLayer` background | `apps/web/lib/use-active-section.ts`, `apps/web/components/three/PosterLayer.tsx`, `apps/web/tests/unit/poster-layer.test.tsx` | **`apps/web/app/layout.tsx`** | T3, T6 | **Visual** |
| 8 | UI primitives | `apps/web/components/ui/Button.tsx`, `Chip.tsx`, `Badge.tsx`, `KpiTile.tsx`, `Monogram.tsx`, `apps/web/tests/unit/ui.test.tsx` | — | T3 | **Visual** |
| 9 | Nav + mobile bottom sheet | `apps/web/components/layout/Nav.tsx`, `appsps/web/lib/nav-items.ts` → **`apps/web/lib/nav-items.ts`**, `apps/web/tests/unit/nav.test.tsx` | — | T6, T8 | **Visual** |
| 10 | Footer | `apps/web/components/layout/Footer.tsx`, `apps/web/tests/unit/footer.test.tsx` | **`apps/web/app/layout.tsx`** (mounts `Nav` + `Footer` + `<main id="main">`) | T4, T6, T9 | **Visual** |
| 11 | Hero + Proof strip | `apps/web/components/sections/Hero.tsx`, `ProofStrip.tsx`, `apps/web/tests/unit/sections-hero.test.tsx` | — | T4, T6, T8 | **Visual** |
| 12 | Selected Work | `apps/web/components/cards/ProjectCard.tsx`, `apps/web/components/sections/SelectedWork.tsx`, `apps/web/tests/unit/sections-work.test.tsx` | — | T4, T8 | **Visual** |
| 13 | How I Lead | `apps/web/lib/leadership.ts`, `apps/web/components/cards/Pillar.tsx`, `apps/web/components/sections/HowILead.tsx`, `apps/web/tests/unit/sections-lead.test.tsx` | — | T6 | **Visual** |
| 14 | Craft + Stack | `apps/web/components/sections/Craft.tsx`, `Stack.tsx`, `apps/web/tests/unit/sections-craft.test.tsx` | — | T4, T8 | **Visual** |
| 15 | Timeline + Writing | `apps/web/lib/format-period.ts`, `apps/web/components/cards/TimelineItem.tsx`, `ArticleCard.tsx`, `apps/web/components/sections/Timeline.tsx`, `Writing.tsx`, `apps/web/tests/unit/sections-timeline.test.tsx` | — | T4 | **Visual** |
| 16 | Contact | `apps/web/components/sections/Contact.tsx`, `apps/web/tests/unit/sections-contact.test.tsx` | — | T4, T8 | **Visual** |
| 17 | Page assembly, metadata, JSON-LD, sitemap | `apps/web/lib/seo.ts`, `apps/web/app/sitemap.ts`, `apps/web/app/robots.ts`, `apps/web/tests/unit/seo.test.ts`, `apps/web/tests/unit/page.test.tsx` | **`apps/web/app/page.tsx`** (full replace), **`apps/web/app/layout.tsx`** (full replace) | T11–T16 | **Structural** (section order is narrative) |
| 18 | E2E, a11y, visual regression | `apps/web/playwright.config.ts`, `apps/web/tests/e2e/home.spec.ts`, `apps/web/tests/e2e/a11y.spec.ts`, `apps/web/tests/visual/home.spec.ts` (+ generated `tests/visual/home.spec.ts-snapshots/`) | **`apps/web/package.json`** (devDeps) | T17 | **Structural** |
| 19 | CI pipeline with budgets | `.github/workflows/ci.yml`, `apps/web/.size-limit.json`, `apps/web/lighthouserc.json` | **root `package.json`**, **`apps/web/package.json`** (scripts `size`, `lighthouse`) | all | **Structural** |

*(Typo note in row 9: the single file is `apps/web/lib/nav-items.ts`.)*

### Collision hot-spots for two concurrent sessions

| File | Written by |
|---|---|
| `apps/web/app/layout.tsx` | **T3 (create) → T7 (edit) → T10 (edit) → T17 (full replace)** — four owners |
| `apps/web/app/page.tsx` | T3 (temporary stub) → T17 (full replace) |
| `apps/web/package.json` | T3 (create) → T18 (devDeps) → T19 (scripts) |
| root `package.json` | T1 (create) → T19 (edit) |
| `apps/web/components/layout/` | T6 (`Container`/`Section`/`SkipLink`) + T9 (`Nav`) + T10 (`Footer`) — same directory, disjoint files |
| `apps/web/lib/` | T4, T5, T7, T9, T13, T15, T17 — disjoint files |
| `apps/web/tests/unit/` | one file per task, disjoint |

**Safe split suggestion:** structural lane = T1, T2, T4, T5, T18, T19 (+ own `apps/web/package.json` script edits last). Visual lane = T3, T6–T17. The two lanes collide only on `apps/web/package.json` and `apps/web/app/layout.tsx`; assign both files to the visual lane and have the structural lane hand over its dependency/script additions as a patch.

---

## Part B — Design delta

### B.0 Which design file is authoritative

`Home v1.dc.html` and `Home.dc.html` describe **two different people's positioning**:

| | `Home v1.dc.html` (older) | `Home.dc.html` (newer, named by the requester) |
|---|---|---|
| Hero eyebrow | `Tech Lead @ Data Age · Senior Software Engineer @ Rapidev Labs · Harare, Zimbabwe` | `Software Engineer @ Data Age · Full Stack Engineer · Harare, Zimbabwe` |
| H1 | `I lead teams that ship production software — and I make the web move.` | `I build production software, end to end — and I make the web move.` |
| Nav item | `How I lead` | `How I work` |
| Sub | Technical direction, code review and mentorship… | Spring Boot microservices, ERP platforms, secure authentication, React front ends… |
| Experience | Data Age (Tech Lead), Rapidev Labs (Senior SWE) | Data Age, **Ikarus 3D**, **Virtualize Technologies**, **Baeldung.com**, Earlier roles |
| Years | 5+ | 6+ |

The M0 plan (and the stored project memory) matches **Home v1**. `Home.dc.html` is a later editorial pivot away from leadership framing toward Java/Spring full-stack framing. **This is the single biggest unresolved question and it must be answered by the owner before Tasks 4, 11, 13, 15 and 17 are written** — it changes `content/profile.json`, `content/experience.json`, the section heading "How I lead", the JSON-LD `jobTitle` assertion in `apps/web/tests/unit/seo.test.ts`, and the site's whole positioning.

Deltas below are stated against `Home.dc.html` + `Foundations.dc.html` (the files named as authoritative), with Home-v1 agreement noted where it matters.

### B.1 Task 3 — tokens and fonts

| ID | Plan says | Design says | Severity |
|---|---|---|---|
| D-01 | `fonts.ts`: `Bricolage_Grotesque({ subsets:['latin'], display:'swap', variable:'--font-bricolage', weight:['400','600','700','800'] })` | Google request `Bricolage+Grotesque:opsz,wdth,wght@12..96,75..100,300..800`, and **every display element sets `font-variation-settings:'wdth' 88 / 90 / 92 / 95 / 96 / 98 / 100, 'opsz' 96`** at `font-weight:600`. Foundations §01 states the rationale outright: *"Bricolage carries a width axis, so display text can be set narrow and tall to echo the Monolith without a second family"* and *"display-1 & display-2 at wdth 88, h2 at wdth 95"* | **Blocking** — pinning `weight:[…]` in `next/font/google` downloads static instances and drops the `wdth`/`opsz` axes. Must be `Bricolage_Grotesque({ subsets:['latin'], display:'swap', variable:'--font-bricolage', axes:['opsz','wdth'] })` (no `weight`), plus per-role `font-variation-settings` utilities. The whole display voice depends on this. |
| D-02 | Display weight in use: `font-extrabold` (800) on the H1 (`Hero.tsx`) | `font-weight:600` everywhere for display; 500 only for the testimonial blockquote | Cosmetic |
| D-03 | `--text-display-1: clamp(2.75rem, 1.5rem + 5vw, 6.5rem)` applied to the H1 | Foundations token export agrees (`--display-1:clamp(2.75rem,1.5rem + 5vw,6.5rem)`), but `Home.dc.html`'s **actual H1 uses `clamp(2.75rem, 1.5rem + 3.6vw, 4.75rem)`** because it lives inside a fixed-width glass panel | Cosmetic (but the plan will over-size the hero once the panel exists) |
| D-04 | Geist via the `geist` npm package (`GeistSans`, variable `--font-geist-sans`) | Geist via Google Fonts `wght@300..700` | Cosmetic — plan's approach is fine; only the weight range differs |
| D-05 | Token set: 17 colours, 3 font vars, 3 radii, 3 text sizes, 2 spacing vars, 1 easing | Design's token export additionally defines: `--gradient:linear-gradient(90deg,#7C3AED 0%,#22D3EE 100%)`, `--glass-bg:rgba(17,22,29,.72)`, `--glass-blur:12px`, `--glass-highlight:rgba(255,255,255,.06)`, `--glow-violet:0 0 24px rgba(124,58,237,.20)`, `--glow-cyan:0 0 24px rgba(34,211,238,.20)`, `--r-pill:999px`, `--h3:1.375rem`, `--body-lg:1.125rem`, `--body:1rem`, `--small:.875rem`, `--label:.75rem`, `--s-1…--s-11` (4→192), `--max-content:1440px`, `--max-bento:1600px`, `--gutter:24px`, `--d-1:120ms --d-2:200ms --d-3:320ms --d-4:560ms`, `--focus` | **Plan-is-silent** on all of them. The glass, glow and pill tokens are load-bearing for Tasks 8–16 — every panel and card in the design uses them |
| D-06 | Colour rule: *"violet = leadership/architecture; cyan = craft/3D. Never mix within one component."* `domainSchema.accent: z.enum(['violet','cyan'])` | `Home.dc.html` domain chips use **six** hues: Healthcare `#6EE7B7`, Education `#FCD34D`, Creator economy `#F0ABFC`, Procurement/ERP `#FCD34D`, Social services `#C4B5FD`, Developer tooling `#67E8F9`, Interactive 3D `#67E8F9`, AR/XR `#F0ABFC`. Stack group headings and timeline dots use the same extended palette. None of `#6EE7B7 / #FCD34D / #F0ABFC / #E879F9` exist in the plan's token list | **Blocking** — `domainSchema.accent` (Task 2, already-canonical, shared with the M1 API) cannot express the design. Either the enum widens (`emerald / amber / fuchsia / violet / cyan …`) or the design reverts to two accents. Decide **before Task 2** or the API contract carries the wrong shape |
| D-07 | `formationIdSchema = z.enum(['monolith','stream','lattice','orbit','scatter','grid','ring','badge'])`; `project.formation` defaults to `'badge'` | Foundations §04 ships **seven** posters: Monolith, Stream, Lattice, Orbit, Scatter, Grid, Ring. There is no `badge` formation anywhere in either design file | Cosmetic — `'badge'` is a plan-only invention with no art direction behind it; harmless but dead |
| D-08 | Focus ring: `:focus-visible { outline: 2px solid var(--color-cyan-400); outline-offset: 2px; border-radius: 2px; }` | `Home.dc.html` applies exactly `outline:2px solid #22D3EE; outline-offset:2px; border-radius:6px;` — but Foundations' token export instead names `--focus:0 0 0 2px var(--bg-0), 0 0 0 4px var(--cyan-400)` (a double box-shadow ring) | Cosmetic — the *applied* design matches the plan; the token export is an unused alternative. Only `border-radius` differs (2px vs 6px) |
| D-09 | `prefers-reduced-motion` handled by a blanket `*{ animation-duration:.01ms !important; transition-duration:.01ms !important }` | Same blanket rule in `Home.dc.html`; Foundations *additionally* zeroes the duration tokens (`--d-1..4: 1ms`) | Cosmetic |
| D-10 | Body: `background`, `color`, `font-family`, antialias, `optimizeLegibility`. No base `font-size`/`line-height` | `font-size:16px; line-height:1.65` on the root; `text-wrap:pretty` on body copy and `text-wrap:balance` on display headings throughout | **Plan-is-silent** — `text-wrap: balance/pretty` is used on essentially every heading and paragraph in the design |
| D-11 | Radii tokens: chip 6 / card 12 / panel 20 | Same three **plus `--r-pill:999px`**, and pills are the dominant button/nav/chip-row shape in `Home.dc.html` | **Blocking for Task 8** (see D-14) |

### B.2 Tasks 6–16 — structure

| ID | Task | Plan says | Design says | Severity |
|---|---|---|---|---|
| D-12 | 7 `PosterLayer` | One `<div aria-hidden class="pointer-events-none fixed inset-0 -z-10">` in the **root layout**, containing 7 absolutely-positioned gradient variants cross-faded by `useActiveSection()` at `duration-[900ms]`, plus an SVG grain overlay | Each `<section>` owns its own `<div aria-hidden style="position:absolute; inset:0"><div style="position:sticky; top:0; height:100vh; …"><canvas data-f="monolith">…</canvas><div style="…scrim gradient…"></div></div></div>`. There is **no single global background layer**. The formation is a per-section *sticky canvas* with a **per-section scrim** (hero: `radial-gradient(80% 110% at 2% 50%, …)`; proof: `linear-gradient(180deg, .82/.62/.82)`; work: `.90/.80/.92`; lead: `.92/.72/.94`; craft: `radial-gradient(70% 100% at 100% 50%, …)`; stack: `.94/.86/.94`; contact: `.90/.70/.92`) | **Blocking** — the plan's whole `PosterLayer` DOM contract (fixed root-level layer, `data-variant`, `useActiveSection`) differs from the design's per-section sticky-canvas contract. Since the plan explicitly says *"M2 replaces the internals with `<PersistentCanvas>`"*, picking the wrong shape now costs M2 a rewrite. Also affects `tests/unit/poster-layer.test.tsx` (asserts 7 `[data-variant]` in one component) and `tests/e2e/a11y.spec.ts` (asserts `toHaveCount(7)`) |
| D-13 | 7 | Poster variant map: `grid → ['stack','timeline','writing']`, so every section has a background | Design gives **`#experience` a flat `background:#0D1117`** and **`#writing` a flat `background:#11161D`** — no canvas, no formation. Only hero/proof/work/lead/craft/stack/contact carry formations (7 sections, 7 formations, 1:1) | **Blocking** (small) — plan's 7-variant → 9-section mapping is a fabrication; the design's mapping is exactly 7:7 with 2 bare sections |
| D-14 | 8 `Button` | `rounded-[var(--radius-chip)]` (**6px**), `min-h-11` (44px), `px-5`, `text-sm`; primary = `bg-fg-0 text-bg-0 hover:bg-violet-300`; secondary = `border-line-2 hover:border-violet-400` | **Pill** `border-radius:999px`; hero CTAs `height:48px; padding:0 26px`; nav/secondary `height:44px; padding:0 22px`. Primary hover = `background:linear-gradient(90deg,#7C3AED 0%,#22D3EE 100%); color:#F0F6FC` (Foundations §02 names this one of *exactly three* sanctioned gradient placements). Secondary hover = `border-color:#22D3EE; color:#F0F6FC` + `box-shadow:0 0 24px rgba(34,211,238,0.20)` | **Blocking** — shape (6px vs pill), hover treatment (violet-300 fill vs gradient), and the missing glow are all wrong |
| D-15 | 8 `Chip` | `text-xs font-medium` in the **body** font; accent variants tint border+text only | All chips are **`font-family:'JetBrains Mono'`**, `0.75rem` (`0.6875rem` on cards), `letter-spacing:0.06em`–`0.12em`, `border-radius:6px`, `padding:6px 11px` / `4px 9px`, with a tinted `background` at 8–10% alpha as well as border+colour. Skill levels render as: core = `color:#F0F6FC; background:rgba(255,255,255,0.06); border:#30363D`; working = `color:#C9D1D9; border:#30363D`; familiar = `color:#8B949E; border:#1F2937` | Cosmetic (font family + fill) — but pervasive |
| D-16 | 8 `Badge` | `Public` / `Client codebase` / `Private codebase` | Same three strings appear (`Public`, `Client codebase`), plus a fourth used on the AR card: **`In preparation`** in warning amber (`#D29922`, `border rgba(210,153,34,0.35)`, `background rgba(210,153,34,0.08)`). Two cards also use the short form `Client` | Cosmetic + **plan-is-silent** on the `In preparation` state, which is how the design surfaces `placeholder: true` |
| D-17 | 8 `Monogram` | Gradient-filled tile: `bg-[linear-gradient(135deg,var(--color-violet-500),var(--color-cyan-400))]`, `rounded-[var(--radius-card)]`, `text-[var(--color-bg-0)]`, `aria-hidden` | Nav mark is an **outlined** box: `width:44px; height:44px; border:1px solid #30363D; border-radius:12px; font-family:'Bricolage Grotesque'; font-variation-settings:'wdth' 90; font-weight:600; color:#F0F6FC`, hover `border-color:#A78BFA`. Footer mark is the same at 40px. **No gradient anywhere on the monogram** | **Blocking** — the gradient monogram would be a fourth gradient placement, which Foundations §02 explicitly forbids (*"appears in exactly three places"*) |
| D-18 | 8 `KpiTile` | `flex-col gap-1 border-l border-line-1 pl-4`; label in mono `0.6875rem`, value in display `text-2xl` | Two distinct KPI presentations: (a) **hero** — `<dl>` 3-col inside the glass panel, label mono `0.75rem`, value Bricolage `wdth 92`, `1.75rem/1.1`, separated by a `border-top:1px solid #1F2937` above; (b) **proof strip** — 4-col `<dl>` with `gap:1px; background:#1F2937; border:1px solid #1F2937; border-radius:12px; overflow:hidden`, each cell `background:rgba(17,22,29,0.86); backdrop-filter:blur(12px); padding:24px`, value `2.25rem`. **Neither uses a left border** | **Blocking** (visual) — one `KpiTile` cannot serve both; needs a `variant` prop |
| D-19 | 9 `Nav` | `NAV_ITEMS = Work, How I lead(#lead), Craft, Stack, **Experience(#timeline)**, Contact(#contact)`; `<header role="banner" class="fixed inset-x-0 top-0">`, `h-18 → h-14` at `scrollY > 80`; primary nav `hidden lg:flex`, `text-sm`, body font; separate `<Button href="#contact">` + hamburger `<dialog>` sheet; monogram links to `#hero` | Nav items: `Work(#work)`, **`How I work(#lead)`**, `Craft(#craft)`, `Stack(#stack)`, **`Experience(#experience)`**, **`Writing(#writing)`** — six items, **no Contact item** (Contact is only the right-hand CTA). `position:sticky; top:0; height:72 → 56px` at **`scrollY > 24`**; background `rgba(13,17,23,0.35) → 0.88`; transition `320ms cubic-bezier(.2,.8,.2,1)` on height/background/border-color. Nav links are **`'JetBrains Mono' 0.75rem uppercase tracking 0.12em`**, `min-height:44px; padding:0 14px; border-radius:999px`, hover `background:rgba(255,255,255,0.04)`. `<nav aria-label="Sections">` (not `"Primary"`). Monogram links to **`#main`**. **No mobile bottom sheet and no hamburger exist in the export** | **Blocking** — `#timeline` vs `#experience` breaks anchors, `useActiveSection`, `SkipLink`/nav e2e specs and the poster mapping. `aria-label="Sections"` vs `"Primary"` breaks `tests/unit/nav.test.tsx` and `tests/e2e/home.spec.ts` (`nav[aria-label="Primary"]`). Label "How I lead" vs "How I work" ties back to B.0. The `<dialog>` sheet is **plan-only** — design is silent on mobile nav (keep the plan's sheet; it is the better answer) |
| D-20 | 10 `Footer` | `<footer role="contentinfo" class="border-t py-16">`; one nav row from `NAV_ITEMS`; primary/secondary links row; colophon paragraph; `elsewhere` links row. No copyright, no monogram | `grid-template-columns:1fr auto`, `padding:64px … 48px`. Left: **40px outlined `WS` monogram + `Wieslaw Samushonga · Harare, Zimbabwe`**, colophon paragraph *(exact string matches the plan's test: "Built with Next.js, React Three Fiber and Rapier. One draw call. Holds 60 fps on a mid-range phone.")*, plus a **`View perf →`** link. Right: `<nav aria-label="Footer">` in **two columns** — col 1 `Work / How I work / Craft / **Lab**`, col 2 `**About** / **Resume** / Writing / Contact`. Bottom bar: `© 2026 Wieslaw Samushonga` in mono uppercase | Cosmetic + **blocking-adjacent**: the design links `/lab`, `/about`, `/resume` — routes the plan's *Definition of done* explicitly excludes from M0. Those links would 404 |
| D-21 | 11 `Hero` | `<Section id="hero" formation="monolith" className="flex min-h-[100svh] items-end pb-24 pt-32 lg:items-center">` → `<Container>` two-column flex: text column `max-w-[24ch]` (eyebrow / h1 / sub / two buttons) and a sibling `<dl data-testid="hero-kpis">` of 3 `KpiTile`s | `<section aria-labelledby="hero-h" style="min-height:100dvh; display:flex; align-items:center">`; sticky monolith canvas + `radial-gradient(80% 110% at 2% 50%, rgba(13,17,23,0.96) → 0)` scrim; content wrapper `padding:160px clamp(1rem,4vw,4rem) 96px`; then a **glass panel** `width:min(56%,700px); background:rgba(17,22,29,0.72); backdrop-filter:blur(12px); border:1px solid #1F2937; border-top:1px solid rgba(255,255,255,0.06); border-radius:20px; padding:48px` containing **eyebrow → h1 → sub → CTA row → `<dl>` 3-col KPIs separated by `border-top:1px solid #1F2937; padding-top:32px`**. Plus an absolutely-positioned **scroll cue** `<a href="#proof" aria-label="Scroll to content">` at `bottom:40px` with a 1px gradient bar animated by `@keyframes wsScrollCue` | **Blocking** — the glass panel is the design's central layout idea (Foundations §03: *"no full-width scrim… text sits on a glass panel with a hard left edge and an open right side"*). The plan has no panel, puts the KPIs outside the text column, and has no scroll cue |
| D-22 | 11 `ProofStrip` | `<Section id="proof" formation="stream" className="border-y … !py-12">`, sr-only h2, chip `<ul>`, then `<dl class="grid grid-cols-2 lg:grid-cols-4">` of `KpiTile`s fed from `profile.kpis` — labels `Experience / Domains shipped / Specialism / Production platforms led` | `padding:48px …`, `border-top`+`border-bottom:1px solid #1F2937`, `aria-label="Domains and figures"` (no sr-only heading), 8 multi-hue domain chips, then the hairline-grid `<dl>` (D-18b) with labels **`Load time cut, Ikarus 3D` = 50%**, `Domains shipped` = 7, **`Frame budget held` = 60 fps**, **`Infra cost cut, Virtualize` = 30%** | **Blocking** (content) — three of four proof KPIs are metrics tied to `Home.dc.html`'s employment history that do not exist in `content/profile.json`. Ties back to B.0 |
| D-23 | 12 `SelectedWork` | `<Container width="bento">`; header = h2 + one paragraph; grid `grid-cols-1 md:grid-cols-2 xl:grid-cols-6` where **index < 2 → `col-span-3`, else `col-span-2`**; cards are `<article>`; each renders domain Chip + Badge + h3 + summary + stack chips + either the first `links[0]` as an external `<a>` or the fallback copy `Architecture walkthrough on request` | Header is a **flex row**: left column (mono eyebrow `01 — Selected work` + h2 + blurb) and a right-aligned **`All work →` pill link to `/work`**. Grid is `repeat(6,1fr)` with spans **3, 3, 4, 2, 2, 3, 3** (gabar, Vantage, we-assist-you, heycreator, learnx, PR-Pulse, AR-placeholder). Cards are **`<a href="/work/<slug>">`** (whole card is the link), `background:rgba(22,27,34,0.78); backdrop-filter:blur(12px); border-top:1px solid rgba(255,255,255,0.06); border-radius:20px; padding:32px/28px`, hover `border-color:#22D3EE|#A78BFA; box-shadow:0 0 24px rgba(…,0.20)`, first two `min-height:340px` with `font-size:2.25rem` h3 and `1.125rem` summary; the rest `1.5rem` h3. Each card ends with `Read the case study →` / `Read →` in mono. The AR card is a **non-interactive `<div aria-label="AR case study, in preparation">`** with `border:1px dashed #30363D`, `In preparation` amber badge, a dotted-underline `[AR project name]` heading, the sentence *"The eighth domain. Card renders live the moment the case study is supplied — the domain count above reads 7 until then."*, and **three empty dashed chip ghosts** | **Blocking** — different span pattern, cards are links to `/work/<slug>` (a route M0 forbids), radius is 20px not 12px, and the AR placeholder is a purpose-built non-card. The plan's `ProjectCard` renders the AR entry as a normal card, contradicting the design |
| D-24 | 13 `HowILead` | Pillars titled **`Technical direction` / `Code review & standards` / `Mentorship & delivery`**, each `title + blurb + 3 practices` with violet dot bullets; 5-step process as `border-t border-violet-500/40` columns; **`renders no testimonial block, because no real quote exists yet`** (test asserts `queryByTestId('testimonials')` is absent) | Eyebrow `02 — How I work`; h2 **`Tested, typed, and shipped with a rollback plan.`**; lede `Three habits that follow me into every codebase.`; pillars are **`Habit 01 Tested by default` / `Habit 02 Architecture that stays legible` / `Habit 03 Owned to production`**, each a glass `<article>` with a coloured `border-top` (violet / **fuchsia `rgba(240,171,252,0.30)`** / cyan) and `▸` bullets in the matching accent. Process is an `<ol>` styled as a **hairline 5-col grid** (`gap:1px; background:#1F2937; border-radius:12px`) with per-step numbers in five different accents. **A testimonial `<figure>` IS rendered** as a dashed placeholder: `Testimonial slot — renders only when a real quote exists`, `[Quote from an engineering manager or client — owner to supply]`, `[Name] · [Role, Company]`, and an 88px dashed circle | **Blocking** — (a) pillar titles/framing are entirely different; (b) the design renders a *designed empty state* for testimonials, which directly contradicts the plan's Global Constraint *"Testimonials are the one exception — that block hides entirely unless a real quote exists"* **and** the plan's unit test. One of the two must give |
| D-25 | 14 `Craft` | `<Container className="flex flex-col gap-8 lg:max-w-[46ch] lg:pl-0">` — left-aligned; h2 `Craft: 3D & AR`; one paragraph; two disabled `Button`s (`Enable physics`, `View in AR`); explanatory mono note | `min-height:88vh; display:flex; align-items:center`; content wrapper `display:flex; justify-content:flex-end` — the panel is **right-aligned**, `width:min(38%,460px); min-width:320px`, glass with `border-top:1px solid rgba(34,211,238,0.30)`. Scrim is `radial-gradient(70% 100% at 100% 50%, …)` (mirrored from the hero). Controls: **three** — `Enable physics` (toggle, `aria-pressed`), `Reset`, and `View in AR` disabled with `title="WebXR is not available in this browser"` **and an inline `Unsupported` badge**. Below a `border-top`: a `Perf HUD · off` toggle button and an **`Open the lab →`** link to `/lab`. Expanding the HUD reveals a mono readout: `fps 60.0 / frame 14.2 ms / draw calls 1 / instances 12 000 / gpu tier 2` | **Blocking** — side, width, control count, the HUD and the `/lab` link are all absent from the plan. (`/lab` is again an M0-forbidden route.) |
| D-26 | 14 `Stack` | Single-column: h2 `Stack` + lede; `grid md:grid-cols-2 xl:grid-cols-3` of 6 skill groups, each `<section data-testid="skill-group-…">` with a mono h3 and level-toned chips | Two-column `grid-template-columns:1fr 380px; gap:64px`. Left: eyebrow `04 — Stack`, h2 **`What I reach for`**, then groups in **`repeat(2,1fr)` with `gap:40px 48px`**, each group heading in a **different accent** (`#C4B5FD`, `#F0ABFC`, `#67E8F9`, `#6EE7B7`, `#22D3EE`, `#FCD34D`), then a **Core / Working / Familiar legend row** with 12px swatches above a `border-top`. Right: a **`position:sticky; top:120px` glass `<aside>` titled `How I choose`** with two paragraphs. Skill *contents* also differ (design lists Java, Kotlin, GLSL, Spring Boot, Hibernate/JPA, GraphQL, MongoDB, Azure, Nginx, JUnit, Mockito, TDD, WebXR — versus the plan's `content/skills.json`) | **Blocking** — layout (sticky aside, 2-col), the legend, per-group accent colours, and the skill inventory itself |
| D-27 | 15 `Timeline` | `<Section id="timeline">`, h2 `Experience`, `<ol>` of `TimelineItem`s; each `<li>` has its **own** `border-l` and a 2×2 violet dot; heading is `{title} · {org}`; period+location in mono below | `<section id="experience">` with **`background:#0D1117` and no formation**; eyebrow `05 — Experience`; h2 **`Where I've done it`**; a single `<ol style="padding-left:32px; border-left:1px solid #1F2937; gap:48px">` — the rail is on the **list**, not each item. Each `<li>` has an absolutely-positioned **12px dot at `left:-38px`** with `background:#0D1117; border:2px solid <accent>; box-shadow:0 0 24px rgba(…,0.40)`, accent varying per entry (`#A78BFA`, `#F0ABFC`, `#67E8F9`, `#6EE7B7`, `#30363D`). Item body is `grid-template-columns:1fr 200px` — org h3 (`1.5rem`, `wdth 96`) + accent-coloured role line + `—` dash bullets, with the **period right-aligned in the second column** as `2024 — present` / `2024` / `2022 — 2024` (year-only, not `Jan 2025 — Present`) | **Blocking** — section id, heading, rail ownership, the two-column grid, the accent-per-entry dots, and **the period format** (`formatPeriod` returns `Jan 2025 — Present`; design shows `2024 — present`). Entry *content* also differs (5 real employers vs the plan's 3) |
| D-28 | 15 `Writing` | `<Section id="writing">`, h2 `Writing & elsewhere`, `md:grid-cols-3` of three `ArticleCard`s built from `content/writing.json` (real titles + excerpts, `placeholder:true`), then one flat `elsewhere` link row from `profile.links` filtered `kind !== 'primary'` | `background:#11161D`, **no formation**; eyebrow `06 — Writing & elsewhere`; h2 **`Notes from the build`**; 3-col grid where **only two are article cards** and the third is a designed **`RSS unavailable`** fallback panel (`border:1px dashed #30363D`, *"The Medium feed didn't respond. Nothing else on the page depends on it."*, `Read on Medium →`). Article titles are **dotted-underline placeholders `[Article title — from RSS]`** with a `Medium · Mar 2026` mono eyebrow, i.e. the design treats writing as *unfetched* rather than as authored placeholder copy. Elsewhere row = pill links `LinkedIn / GitHub / X / Medium`, a 1px divider, then mono links `Instagram / Discord / Reddit / Pinterest` | **Blocking** (moderate) — 2 cards + 1 fallback vs 3 cards; the plan's test asserts `getAllByRole('article')` length equals `getWriting().length`. Also `Discord` and `Pinterest` are not in `content/profile.json`; `Rapidev Labs` is in the plan but not the design |
| D-29 | 16 `Contact` | Two-column flex: left header (h2 = `profile.availability`, paragraph, `Copy email` button + `aria-live` "Copied"); right `<form>` with `Your name` / `Email` / `Message` and a **`<a href={mailto}>Send message</a>`**, plus the note `Opens your mail client. Direct submission arrives with the API.` | `grid-template-columns:1fr clamp(320px,38vw,520px)`. Left: eyebrow `07 — Contact` (cyan), h2 `Open to consulting & collaboration.` (`clamp(2rem,1.2rem + 2vw,3.25rem)`, `max-width:16ch`), paragraph *(matches the plan's copy verbatim)*, then a **hairline-grid contact card** (`gap:1px; background:#1F2937; border-radius:12px`) with three rows: a **Copy-email `<button>` showing the address and a `Copy`/`Copied` affordance**, a LinkedIn row, and a GitHub row (each with a `↗`). Right: a glass `<form>` with labels `Name` / `Email` / `Message` (`placeholder="What are you building?"`, `rows=5`), a real **`<button type="submit">`** with states `Send message / Sending… / Sent / Try again`, inline `role="alert"` email validation (`Enter an email address I can reply to.`), a `role="status"` result banner with success/error/limited/**offline** variants, and the note *"No newsletter, no tracking pixel. Replies usually within a working day, CAT."* | **Blocking** — the design ships the *API-connected* form (M3), the plan ships the mailto-only fallback (correct for M0). The two disagree on: element type (`<a>` vs `<button>`), label text (`Your name` vs `Name`), the copy-email affordance (standalone button vs a row inside the contact card), the closing note, and field styling (`height:48px; border-radius:12px; background:#0D1117; border:#30363D`, focus `border-color:#22D3EE` — the plan uses `radius-chip` 6px on `bg-1`). **M0 should build the design's markup with only the `offline` state active** |
| D-30 | all | No reveal/entrance animation anywhere; M0 explicitly excludes the Motion library | Every card, panel, grid and list carries **`data-reveal`**: hidden below the fold at `opacity:0; translateY(24px)`, revealed by IntersectionObserver at `threshold:0.12, rootMargin:'0px 0px -4% 0px'` with `transition: opacity 560ms cubic-bezier(.2,.8,.2,1) <i*70>ms, transform …` (70ms stagger between siblings), skipped entirely under `prefers-reduced-motion`, with a 4s geometry-fallback timer so nothing can stay invisible | **Plan-is-silent** — implementable in M0 with plain CSS + IO (no Motion dependency), and it is the design's main sense of life. Worth adding as a small shared hook |
| D-31 | 6 `Section` | `<section id data-section data-formation aria-labelledby class="relative py-[var(--spacing-section)]">` | Sections use `padding: clamp(4rem,10vw,10rem) clamp(1rem,4vw,4rem)` on an **inner** wrapper that also carries `max-width` and `margin:0 auto` — i.e. the design merges Container+Section padding into one element, and adds `border-top:1px solid #1F2937` between most sections. Hero and Craft additionally set `min-height:100dvh` / `88vh` + `display:flex; align-items:center` | Cosmetic — the plan's split is fine, but `Section` needs a `border-top` affordance and the `min-height`/centering variants |
| D-32 | 6 `SkipLink` | `sr-only focus:not-sr-only focus:fixed focus:left-4 focus:top-4 …` | `position:absolute; left:-9999px; top:16px; z-index:100; background:#161B22; border:1px solid #22D3EE; border-radius:6px; padding:12px 20px; color:#F0F6FC`, focus → `left:24px` | Cosmetic |
| D-33 | 10/17 `layout` | `<body><SkipLink/><PosterLayer/><Nav/><main id="main">{children}</main><Footer/>…</body>` | Same order (skip link → header → `<main id="main">` → footer), minus the global poster layer (see D-12). Nav monogram targets `#main`, and the hero has **no `id="hero"`** — the design's first section is unnamed and `#proof` is the first anchor | Cosmetic + **plan-is-silent**: plan's `Nav` links the monogram to `#hero`, design to `#main`; plan's page test expects `data-section="hero"` first |

---

## Part C — Task 1 verification

**Verdict: content is byte-for-byte identical to the plan. Two process gaps, one plan defect.**

Checked on disk at `C:\Users\wiesl\OneDrive\Documents\Github\JxstWieslaw\portfolio\`:

| File | Plan spec | On disk | Result |
|---|---|---|---|
| `package.json` | 500 B, name/private/packageManager/engines/7 scripts/3 devDeps | identical | ✅ exact |
| `pnpm-workspace.yaml` | `apps/*`, `packages/*` | identical | ✅ exact |
| `turbo.json` | `ui:"stream"` + 7 tasks | identical | ✅ exact |
| `.npmrc` | 2 lines | identical | ✅ exact |
| `.nvmrc` | `22` | identical | ✅ exact |
| `.gitignore` | 11 entries | identical | ✅ exact |
| `packages/config/package.json` | `@repo/config`, files/exports/3 devDeps | identical | ✅ exact |
| `packages/config/tsconfig.base.json` | 15 compilerOptions | identical | ✅ exact |
| `packages/config/eslint.config.mjs` | `tseslint.config(...)` + 3 rules + ignores | identical | ✅ exact |

**Step 4 verification re-run and confirmed:** `npx turbo run typecheck --dry=json` returns a clean graph, `"packages": ["@repo/config"]`, no errors. Installed turbo is **2.10.10** (spec `^2.3.0` — satisfied). `node_modules/` and `pnpm-lock.yaml` (33 645 B) exist, so `pnpm install` ran.

### Drift

1. **Step 5 (commit) has not been done.** `git status` shows every Task 1 artefact still untracked on `feat/m0-foundations`:
   `?? portfolio/.gitignore`, `?? portfolio/.npmrc`, `?? portfolio/.nvmrc`, `?? portfolio/package.json`, `?? portfolio/packages/`, `?? portfolio/pnpm-lock.yaml`, `?? portfolio/pnpm-workspace.yaml`, `?? portfolio/turbo.json`.
   The plan's Global Constraints require *"Commit after every task."* Task 1 is functionally complete but not committed.

2. **Plan defect — `pnpm-lock.yaml` is missing from the Task 1 `git add`.**
   ```
   git add package.json pnpm-workspace.yaml turbo.json .npmrc .nvmrc .gitignore packages/config
   ```
   omits `pnpm-lock.yaml`, yet Task 19's CI runs `pnpm install --frozen-lockfile` in all three jobs, which **fails hard without a committed lockfile**. Add `pnpm-lock.yaml` to the Step 5 `git add` (or use `git add -A` from `portfolio/`).

3. **Working-directory assumption.** The plan says "Run from `portfolio/`" and `git checkout -b feat/m0-foundations` — but the git root is one level up (`JxstWieslaw/`), so every `git add` in the plan is relative to `portfolio/` while the branch is repo-wide. Harmless in practice (the branch already exists and is correct), but any agent running `git add` from the repo root will need the `portfolio/` prefix.

4. **`.turbo/` scratch directory** now exists at `portfolio/.turbo` — covered by the created `.gitignore`. No action.

5. **Not drift, worth noting:** `packages/config/` has no `scripts`, so `turbo run typecheck` currently resolves to zero executable tasks. The plan calls this out as correct at this stage.

---

## Recommended decision order (before more code is written)

1. **B.0 — which positioning?** Tech Lead/leadership (`Home v1`, the plan, the stored memory) or full-stack Java/Spring (`Home.dc.html`). Gates Tasks 4, 11, 13, 15, 17.
2. **D-06** — widen `domainSchema.accent` or revert the design to two accents. Gates **Task 2**, which is the API-shared contract.
3. **D-12/D-13** — global fixed `PosterLayer` vs per-section sticky canvas. Gates Tasks 6, 7 and everything M2.
4. **D-19** — `#timeline` vs `#experience`, nav labels, `aria-label="Primary"` vs `"Sections"`. Gates Tasks 9, 15, 17, 18.
5. **D-01** — Bricolage variable axes. Gates Task 3 and every heading thereafter.
6. **D-24** — does the testimonial slot render as a designed placeholder (design) or not at all (plan Global Constraint)? Gates Task 13.
7. **D-20/D-23/D-25** — the design links `/work/<slug>`, `/lab`, `/about`, `/resume`; M0's Definition of done forbids those routes. Decide: stub routes, or drop the links in M0.
